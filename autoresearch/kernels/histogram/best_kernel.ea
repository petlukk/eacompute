export func histogram_i32(data: *i32, hist: *mut i32, len: i32) {
    let mut i: i32 = 0
    while i < 256 {
        hist[i] = 0
        i = i + 1
    }
    i = 0
    while i < len {
        let val: i32 = data[i]
        hist[val] = hist[val] + 1
        i = i + 1
    }
}
